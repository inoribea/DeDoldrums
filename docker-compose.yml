services:
  bridge:
    build: .
    container_name: research-agent
    restart: unless-stopped
    ports:
      - "18765:18765"
    env_file:
      - .env
    volumes:
      - ./memory:/app/memory
    environment:
      - BRIDGE_HOST=0.0.0.0
      - BRIDGE_PORT=18765
